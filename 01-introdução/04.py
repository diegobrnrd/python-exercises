# 4. A imobiliária XYZ vende apenas terrenos retangulares.
# Faça um programa para ler as dimensões de um terreno e exibir a área do mesmo.

# A = b.h

base = float(input('Digite a base do terreno: '))
altura = float(input('Digite a altura do terreno: '))

print(f'Área do terreno: {base * altura}')
